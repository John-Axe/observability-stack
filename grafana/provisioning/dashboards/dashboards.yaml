apiVersion: 1

providers:
  - name: findings
    orgId: 1
    folder: "Security Findings"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
